Script InfoSlashCmd_nanoforge {
cmd "if walletUnlocked HCube menucmd UseHCube"
cmd "if walletLocked HCube gamemsg log You don't own a Nanoforge."
}
Script InfoSlashCmd_forge {
cmd "if walletUnlocked HCube menucmd UseHCube"
cmd "if walletLocked HCube gamemsg log You don't own a Nanoforge."
}
Script InfoSlashCmd_nano {
cmd "if walletUnlocked HCube menucmd UseHCube"
cmd "if walletLocked HCube gamemsg log You don't own a Nanoforge."
}
